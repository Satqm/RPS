<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=yes, viewport-fit=cover">
    <title>R.P.S. School – Practical Lesson Planner</title>
    <style>
        :root {
            --green-950: #0a2e18;
            --green-900: #0d3b1e;
            --green-800: #14522b;
            --green-700: #1a6b3c;
            --green-600: #228b45;
            --green-500: #2e8b4e;
            --green-400: #3da066;
            --green-300: #6dbf85;
            --green-200: #a5d6a7;
            --green-100: #c8e6c9;
            --green-50: #e8f5e9;
            --green-25: #f2faf4;
            --amber-600: #b45309;
            --amber-500: #d69e2e;
            --amber-400: #e8b84b;
            --amber-300: #f0c96d;
            --amber-100: #fef9c3;
            --amber-50: #fffdf0;
            --red-500: #e53e3e;
            --red-400: #fc8181;
            --red-100: #fee2e2;
            --red-50: #fff5f5;
            --blue-600: #2563eb;
            --blue-500: #3182ce;
            --blue-400: #60a5fa;
            --blue-100: #dbeafe;
            --blue-50: #eff6ff;
            --purple-500: #7c3aed;
            --purple-100: #ede9fe;
            --gray-900: #1a1d1a;
            --gray-800: #1f2937;
            --gray-700: #374151;
            --gray-600: #4b5563;
            --gray-500: #6b7280;
            --gray-400: #9ca3af;
            --gray-300: #d1d5db;
            --gray-200: #e5e7eb;
            --gray-100: #f3f4f6;
            --gray-50: #f9fafb;
            --white: #ffffff;
            --shadow-xs: 0 1px 2px rgba(0, 0, 0, 0.03);
            --shadow-sm: 0 1px 3px rgba(0, 0, 0, 0.06);
            --shadow: 0 2px 8px rgba(0, 0, 0, 0.07);
            --shadow-md: 0 4px 16px rgba(0, 0, 0, 0.10);
            --shadow-lg: 0 8px 32px rgba(0, 0, 0, 0.14);
            --shadow-xl: 0 16px 48px rgba(0, 0, 0, 0.18);
            --radius-xs: 5px;
            --radius-sm: 8px;
            --radius: 11px;
            --radius-md: 14px;
            --radius-lg: 18px;
            --radius-xl: 22px;
            --radius-2xl: 28px;
            --transition-fast: 0.15s ease;
            --transition: 0.22s ease;
            --transition-slow: 0.3s ease;
            --font-head: 'Segoe UI', system-ui, -apple-system, sans-serif;
            --font-body: 'Segoe UI', system-ui, -apple-system, sans-serif;
            --sidebar-w: 240px;
            --topbar-h: 60px;
        }
        .dark-mode {
            --green-950: #e8f5e9;
            --green-900: #c8e6c9;
            --green-800: #a5d6a7;
            --green-700: #81c784;
            --green-600: #66bb6a;
            --green-500: #4caf50;
            --green-400: #43a047;
            --green-300: #388e3c;
            --green-200: #2e7d32;
            --green-100: #1b5e20;
            --green-50: #1a3c24;
            --green-25: #162e1d;
            --amber-600: #f59e0b;
            --amber-500: #f59e0b;
            --amber-400: #d97706;
            --amber-300: #b45309;
            --amber-100: #2e2818;
            --amber-50: #1f1c10;
            --red-500: #ef4444;
            --red-400: #f87171;
            --red-100: #2e1a1a;
            --red-50: #1f1616;
            --blue-600: #60a5fa;
            --blue-500: #60a5fa;
            --blue-400: #3b82f6;
            --blue-100: #1a2230;
            --blue-50: #141e2a;
            --purple-500: #a78bfa;
            --purple-100: #1e1a2e;
            --gray-900: #f3f4f6;
            --gray-800: #e5e7eb;
            --gray-700: #d1d5db;
            --gray-600: #9ca3af;
            --gray-500: #6b7280;
            --gray-400: #4b5563;
            --gray-300: #374151;
            --gray-200: #262e35;
            --gray-100: #1f262b;
            --gray-50: #181c1f;
            --white: #1a1d1a;
            --shadow-xs: 0 1px 2px rgba(0, 0, 0, 0.2);
            --shadow-sm: 0 1px 3px rgba(0, 0, 0, 0.25);
            --shadow: 0 2px 8px rgba(0, 0, 0, 0.3);
            --shadow-md: 0 4px 16px rgba(0, 0, 0, 0.35);
            --shadow-lg: 0 8px 32px rgba(0, 0, 0, 0.4);
            --shadow-xl: 0 16px 48px rgba(0, 0, 0, 0.5);
        }
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        html {
            font-size: 15px;
            scroll-behavior: smooth;
        }
        body {
            font-family: var(--font-body);
            background: var(--gray-50);
            color: var(--gray-900);
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            transition: background var(--transition-slow), color var(--transition-slow);
            -webkit-tap-highlight-color: transparent;
            -webkit-font-smoothing: antialiased;
            line-height: 1.5;
        }
        .topbar {
            position: sticky;
            top: 0;
            z-index: 300;
            height: var(--topbar-h);
            background: linear-gradient(135deg, #0d3b1e 0%, #1a6b3c 60%, #228b45 100%);
            display: flex;
            align-items: center;
            padding: 0 16px;
            gap: 12px;
            box-shadow: 0 3px 16px rgba(0, 0, 0, 0.16);
            transition: background var(--transition-slow);
        }
        .dark-mode .topbar {
            background: linear-gradient(135deg, #0a1a10 0%, #0f2a1a 60%, #153520 100%);
        }
        .topbar-logo {
            width: 40px;
            height: 40px;
            border-radius: 50%;
            background: rgba(255, 255, 255, 0.13);
            border: 2px solid rgba(255, 255, 255, 0.26);
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 1.1rem;
            font-weight: 800;
            color: #fff;
            flex-shrink: 0;
            letter-spacing: -0.5px;
            backdrop-filter: blur(4px);
        }
        .topbar-brand {
            flex: 1;
            min-width: 0;
        }
        .topbar-school {
            font-weight: 700;
            font-size: 1rem;
            color: #fff;
            letter-spacing: 0.01em;
            line-height: 1.2;
            white-space: nowrap;
        }
        .topbar-sub {
            font-size: 0.58rem;
            color: rgba(255, 255, 255, 0.7);
            letter-spacing: 0.06em;
            text-transform: uppercase;
        }
        .topbar-right {
            display: flex;
            align-items: center;
            gap: 8px;
            flex-shrink: 0;
        }
        .topbar-chip {
            font-size: 0.62rem;
            font-weight: 600;
            background: rgba(255, 255, 255, 0.13);
            color: rgba(255, 255, 255, 0.9);
            padding: 4px 9px;
            border-radius: 14px;
            letter-spacing: 0.03em;
            white-space: nowrap;
            border: 1px solid rgba(255, 255, 255, 0.14);
        }
        .topbar-teacher {
            font-size: 0.7rem;
            color: rgba(255, 255, 255, 0.86);
            font-weight: 500;
            display: flex;
            align-items: center;
            gap: 5px;
            white-space: nowrap;
        }
        .dot-live {
            width: 7px;
            height: 7px;
            background: #4caf50;
            border-radius: 50%;
            animation: pulse-dot 2s infinite;
        }
        @keyframes pulse-dot {
            0%,
            100% {
                box-shadow: 0 0 0 0 rgba(76, 175, 80, 0.7);
            }
            50% {
                box-shadow: 0 0 0 10px rgba(76, 175, 80, 0);
            }
        }
        .topbar-date {
            font-size: 0.66rem;
            color: rgba(255, 255, 255, 0.76);
            white-space: nowrap;
            display: none;
        }
        @media (min-width: 600px) {
            .topbar-date {
                display: inline;
            }
        }
        .hamburger {
            display: none;
            all: unset;
            cursor: pointer;
            width: 36px;
            height: 36px;
            border-radius: 50%;
            background: rgba(255, 255, 255, 0.1);
            align-items: center;
            justify-content: center;
            font-size: 1.2rem;
            color: #fff;
            flex-shrink: 0;
            transition: var(--transition-fast);
            border: 1px solid rgba(255, 255, 255, 0.16);
        }
        .hamburger:hover {
            background: rgba(255, 255, 255, 0.2);
        }
        .app-shell {
            display: flex;
            flex: 1;
            position: relative;
        }
        .sidebar {
            position: fixed;
            top: var(--topbar-h);
            left: 0;
            bottom: 0;
            width: var(--sidebar-w);
            background: var(--white);
            border-right: 1px solid var(--gray-200);
            z-index: 250;
            box-shadow: var(--shadow);
            display: flex;
            flex-direction: column;
            overflow-y: auto;
            transition: transform var(--transition-slow), background var(--transition-slow);
            transform: translateX(0);
        }
        .sidebar.closed {
            transform: translateX(-100%);
        }
        .sidebar-header {
            padding: 14px 14px 10px;
            border-bottom: 1px solid var(--gray-100);
            text-align: center;
        }
        .sidebar-header .session-badge {
            font-size: 0.58rem;
            font-weight: 700;
            background: var(--green-25);
            color: var(--green-700);
            padding: 4px 9px;
            border-radius: 10px;
            letter-spacing: 0.05em;
            text-transform: uppercase;
        }
        .sidebar-nav {
            flex: 1;
            padding: 4px 0;
            display: flex;
            flex-direction: column;
            gap: 1px;
        }
        .sidebar-nav button {
            all: unset;
            display: flex;
            align-items: center;
            gap: 9px;
            padding: 10px 14px;
            cursor: pointer;
            font-size: 0.82rem;
            font-weight: 500;
            color: var(--gray-700);
            border-left: 3px solid transparent;
            transition: all var(--transition-fast);
            border-radius: 0 6px 6px 0;
            margin-right: 5px;
        }
        .sidebar-nav button:hover {
            background: var(--green-25);
            color: var(--green-700);
        }
        .sidebar-nav button.active {
            background: var(--green-50);
            color: var(--green-800);
            border-left-color: var(--green-500);
            font-weight: 600;
        }
        .nav-icon {
            width: 20px;
            text-align: center;
            font-size: 0.95rem;
            flex-shrink: 0;
        }
        .sidebar-footer {
            padding: 9px 12px;
            border-top: 1px solid var(--gray-100);
            font-size: 0.6rem;
            color: var(--gray-400);
            text-align: center;
            line-height: 1.4;
        }
        .sidebar-overlay {
            display: none;
            position: fixed;
            inset: 0;
            top: var(--topbar-h);
            background: rgba(0, 0, 0, 0.5);
            z-index: 240;
            backdrop-filter: blur(2px);
        }
        .sidebar-overlay.active {
            display: block;
        }
        .main-area {
            margin-left: var(--sidebar-w);
            flex: 1;
            min-height: calc(100vh - var(--topbar-h));
            transition: margin var(--transition-slow);
            display: flex;
            flex-direction: column;
        }
        .content {
            flex: 1;
            padding: 20px 22px;
            max-width: 1150px;
            width: 100%;
            margin: 0 auto;
        }
        @media (max-width: 900px) {
            .sidebar {
                transform: translateX(-100%);
                box-shadow: var(--shadow-xl);
            }
            .sidebar.open {
                transform: translateX(0);
            }
            .main-area {
                margin-left: 0 !important;
            }
            .hamburger {
                display: flex;
            }
            .content {
                padding: 10px 8px;
            }
        }
        @media print {
            @page {
                margin: 10mm 8mm;
                size: A4 portrait;
            }
            body {
                background: #fff !important;
                color: #000 !important;
                font-size: 11px;
            }
            .sidebar,
            .topbar,
            .action-bar,
            .btn,
            .modal-overlay,
            .search-input,
            .sidebar-overlay,
            .hamburger,
            .card-actions-row,
            .toast,
            .suggestion-box,
            .calendar-nav-row,
            .calendar-grid,
            .calendar-legend,
            .date-chip,
            .period-chip,
            .status-pill,
            .activity-badge-inline,
            .notes-snippet,
            .section-heading .accent-dot {
                display: none !important;
            }
            .main-area {
                margin-left: 0 !important;
                min-height: auto !important;
            }
            .content {
                padding: 0 !important;
                max-width: 100% !important;
            }
            .view {
                display: block !important;
                animation: none !important;
            }
            .period-grid {
                display: block !important;
            }
            .period-card {
                break-inside: avoid;
                box-shadow: none !important;
                border: 1px solid #bbb !important;
                background: #fff !important;
                border-radius: 5px !important;
                padding: 7px 10px !important;
                margin-bottom: 5px !important;
            }
            .period-card::before {
                display: none !important;
            }
            .print-only-header {
                display: block !important;
                text-align: center;
                margin-bottom: 12px;
                border-bottom: 2px solid #000;
                padding-bottom: 6px;
            }
            .print-only-header h1 {
                font-size: 1rem;
                font-weight: 700;
                margin: 0;
            }
            .print-only-header p {
                font-size: 0.65rem;
                color: #333;
                margin: 1px 0 0;
            }
            .print-period-table-wrap {
                display: block !important;
                margin-top: 16px;
                page-break-before: always;
            }
            .print-period-table-wrap h3 {
                font-size: 0.8rem;
                margin-bottom: 6px;
            }
            .print-period-table {
                width: 100%;
                border-collapse: collapse;
                font-size: 0.65rem;
            }
            .print-period-table th,
            .print-period-table td {
                border: 1px solid #999;
                padding: 4px 6px;
                text-align: left;
            }
            .print-period-table th {
                background: #e8e8e8;
                font-weight: 700;
            }
            .print-summary-box {
                display: block !important;
                margin-top: 10px;
                padding: 8px 12px;
                border: 1px solid #ccc;
                border-radius: 5px;
                background: #fafafa;
                font-size: 0.68rem;
            }
            .section-heading h2 {
                font-size: 0.9rem !important;
                color: #000 !important;
            }
        }
        .print-only-header {
            display: none;
        }
        .print-period-table-wrap {
            display: none;
        }
        .print-summary-box {
            display: none;
        }
        .view {
            display: none;
            animation: viewFadeIn 0.26s ease;
        }
        .view.active {
            display: block;
        }
        @keyframes viewFadeIn {
            from {
                opacity: 0;
                transform: translateY(6px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }
        .section-heading {
            display: flex;
            align-items: center;
            gap: 9px;
            margin-bottom: 14px;
            flex-wrap: wrap;
        }
        .section-heading h2 {
            font-size: 1.2rem;
            font-weight: 700;
            color: var(--gray-900);
            letter-spacing: -0.01em;
        }
        .accent-dot {
            width: 8px;
            height: 8px;
            border-radius: 50%;
            background: var(--green-500);
            flex-shrink: 0;
        }
        .date-chip {
            font-size: 0.72rem;
            font-weight: 500;
            background: var(--gray-100);
            color: var(--gray-600);
            padding: 4px 11px;
            border-radius: 14px;
            border: 1px solid var(--gray-200);
        }
        .action-bar {
            display: flex;
            gap: 6px;
            flex-wrap: wrap;
            margin-bottom: 14px;
            align-items: center;
        }
        .btn {
            all: unset;
            display: inline-flex;
            align-items: center;
            gap: 4px;
            padding: 7px 14px;
            border-radius: 16px;
            cursor: pointer;
            font-size: 0.76rem;
            font-weight: 600;
            transition: all var(--transition-fast);
            border: 1.5px solid transparent;
            white-space: nowrap;
            letter-spacing: 0.01em;
            user-select: none;
            line-height: 1.3;
        }
        .btn:active {
            transform: scale(0.94);
        }
        .btn-sm {
            padding: 4px 9px;
            font-size: 0.68rem;
            border-radius: 12px;
        }
        .btn-xs {
            padding: 3px 7px;
            font-size: 0.63rem;
            border-radius: 9px;
        }
        .btn-primary {
            background: var(--green-500);
            color: #fff;
            box-shadow: var(--shadow-xs);
        }
        .btn-primary:hover {
            background: var(--green-700);
            box-shadow: var(--shadow-md);
            transform: translateY(-1px);
        }
        .btn-outline {
            border: 1.5px solid var(--gray-300);
            color: var(--gray-700);
            background: var(--white);
        }
        .btn-outline:hover {
            background: var(--green-25);
            border-color: var(--green-300);
            color: var(--green-700);
        }
        .btn-ghost {
            color: var(--gray-500);
            background: transparent;
        }
        .btn-ghost:hover {
            background: var(--gray-100);
            color: var(--gray-800);
        }
        .btn-warn {
            background: var(--amber-500);
            color: #fff;
        }
        .btn-warn:hover {
            background: #b7791f;
            box-shadow: var(--shadow-md);
            transform: translateY(-1px);
        }
        .btn-success {
            background: var(--green-400);
            color: #fff;
        }
        .btn-success:hover {
            background: var(--green-600);
            box-shadow: var(--shadow-md);
            transform: translateY(-1px);
        }
        .btn-danger {
            background: var(--red-100);
            color: var(--red-500);
            border: 1.5px solid var(--red-100);
        }
        .btn-danger:hover {
            background: var(--red-500);
            color: #fff;
            border-color: var(--red-500);
        }
        .btn-gold {
            background: linear-gradient(135deg, #c9a84c, #d69e2e);
            color: #fff;
            box-shadow: var(--shadow-sm);
        }
        .btn-gold:hover {
            box-shadow: var(--shadow-md);
            transform: translateY(-1px);
            filter: brightness(1.07);
        }
        .search-input {
            padding: 7px 13px;
            border: 1.5px solid var(--gray-300);
            border-radius: 16px;
            font-size: 0.78rem;
            background: var(--white);
            color: var(--gray-800);
            outline: none;
            min-width: 150px;
            font-family: var(--font-body);
            transition: all var(--transition-fast);
        }
        .search-input:focus {
            border-color: var(--green-400);
            box-shadow: 0 0 0 3px rgba(61, 160, 102, 0.07);
        }
        .suggestion-box {
            background: linear-gradient(135deg, #0d3b1e 0%, #1a5c32 50%, #1a6b3c 100%);
            border-radius: var(--radius-lg);
            padding: 14px 18px;
            margin-bottom: 12px;
            color: #fff;
            box-shadow: var(--shadow-md);
        }
        .dark-mode .suggestion-box {
            background: linear-gradient(135deg, #0d2818 0%, #1a3d24 100%);
        }
        .suggestion-title {
            font-weight: 700;
            font-size: 0.82rem;
            margin-bottom: 7px;
            opacity: 0.94;
            letter-spacing: 0.01em;
        }
        .suggestion-chips {
            display: flex;
            flex-wrap: wrap;
            gap: 6px;
        }
        .suggestion-chip {
            background: rgba(255, 255, 255, 0.14);
            border: 1px solid rgba(255, 255, 255, 0.2);
            border-radius: 14px;
            padding: 4px 10px;
            font-size: 0.7rem;
            font-weight: 500;
            cursor: pointer;
            transition: all var(--transition-fast);
            backdrop-filter: blur(2px);
        }
        .suggestion-chip:hover {
            background: rgba(255, 255, 255, 0.25);
            transform: translateY(-1px);
        }
        .period-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(310px, 1fr));
            gap: 10px;
        }
        @media (max-width: 600px) {
            .period-grid {
                grid-template-columns: 1fr;
                gap: 8px;
            }
        }
        .period-card {
            background: var(--white);
            border: 1px solid var(--gray-200);
            border-radius: var(--radius-md);
            padding: 14px 16px;
            box-shadow: var(--shadow-sm);
            transition: all var(--transition);
            position: relative;
            overflow: hidden;
            cursor: default;
        }
        .period-card::before {
            content: '';
            position: absolute;
            left: 0;
            top: 0;
            bottom: 0;
            width: 4px;
            background: var(--gray-300);
            border-radius: 0 3px 3px 0;
            transition: all var(--transition);
        }
        .period-card.completed::before {
            background: var(--green-400);
            width: 5px;
        }
        .period-card.skipped::before {
            background: var(--amber-400);
            width: 5px;
        }
        .period-card.activity-card::before {
            background: var(--purple-500);
            width: 5px;
        }
        .period-card.completed {
            background: var(--green-25);
            border-color: var(--green-100);
        }
        .period-card.skipped {
            background: var(--amber-50);
            border-color: var(--amber-100);
        }
        .period-card.activity-card {
            background: var(--purple-100);
            border-color: #ddd6fe;
        }
        .dark-mode .period-card.activity-card {
            background: #1e1a2e;
            border-color: #3b2e5a;
        }
        .period-card:hover {
            box-shadow: var(--shadow-md);
            border-color: var(--gray-300);
            transform: translateY(-1px);
        }
        .period-chip {
            display: inline-block;
            font-size: 0.62rem;
            font-weight: 700;
            background: var(--gray-800);
            color: #fff;
            padding: 3px 8px;
            border-radius: 9px;
            letter-spacing: 0.03em;
            margin-bottom: 3px;
        }
        .period-card.completed .period-chip {
            background: var(--green-700);
        }
        .period-card.skipped .period-chip {
            background: var(--amber-500);
        }
        .period-card.activity-card .period-chip {
            background: var(--purple-500);
        }
        .class-title {
            font-size: 0.95rem;
            font-weight: 700;
            color: var(--gray-900);
            margin: 2px 0;
            letter-spacing: -0.01em;
        }
        .subject-tag {
            font-size: 0.76rem;
            color: var(--gray-600);
            display: flex;
            align-items: center;
            gap: 4px;
        }
        .activity-badge-inline {
            display: inline-block;
            font-size: 0.6rem;
            font-weight: 700;
            background: #ede9fe;
            color: #5b21b6;
            padding: 2px 7px;
            border-radius: 7px;
            letter-spacing: 0.04em;
            text-transform: uppercase;
            margin-left: 5px;
            vertical-align: middle;
        }
        .dark-mode .activity-badge-inline {
            background: #3b2e5a;
            color: #c4b5fd;
        }
        .status-pill {
            font-size: 0.63rem;
            padding: 2px 7px;
            border-radius: 8px;
            font-weight: 600;
            display: inline-block;
            letter-spacing: 0.02em;
            margin-top: 2px;
        }
        .status-not-started {
            background: var(--gray-200);
            color: var(--gray-600);
        }
        .status-completed {
            background: #d4edda;
            color: #155724;
        }
        .status-skipped {
            background: #fff3cd;
            color: #856404;
        }
        .dark-mode .status-completed {
            background: #1e3a28;
            color: #81c784;
        }
        .dark-mode .status-skipped {
            background: #2e2818;
            color: #fbbf24;
        }
        .dark-mode .status-not-started {
            background: #2a3035;
            color: #9ca3af;
        }
        .notes-snippet {
            font-size: 0.68rem;
            color: var(--gray-500);
            font-style: italic;
            margin-top: 2px;
            line-height: 1.3;
            max-height: 2.4em;
            overflow: hidden;
            text-overflow: ellipsis;
        }
        .card-actions-row {
            display: flex;
            gap: 4px;
            margin-top: 8px;
            flex-wrap: wrap;
        }
        .card-actions-row .btn {
            font-size: 0.65rem;
            padding: 3px 8px;
        }
        .modal-overlay {
            position: fixed;
            inset: 0;
            background: rgba(0, 0, 0, 0.55);
            z-index: 500;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 12px;
            backdrop-filter: blur(3px);
        }
        .modal-overlay.hidden {
            display: none;
        }
        .modal {
            background: var(--white);
            border-radius: var(--radius-xl);
            padding: 20px 22px;
            max-width: 460px;
            width: 100%;
            box-shadow: var(--shadow-xl);
            max-height: 88vh;
            overflow-y: auto;
            animation: modalSlideIn 0.24s ease;
        }
        @keyframes modalSlideIn {
            from {
                opacity: 0;
                transform: translateY(16px) scale(0.97);
            }
            to {
                opacity: 1;
                transform: translateY(0) scale(1);
            }
        }
        .modal h3 {
            margin-bottom: 10px;
            color: var(--green-800);
            font-size: 1rem;
            font-weight: 700;
            letter-spacing: -0.01em;
        }
        .modal label {
            display: block;
            font-weight: 600;
            font-size: 0.66rem;
            margin: 7px 0 2px;
            color: var(--gray-500);
            text-transform: uppercase;
            letter-spacing: 0.04em;
        }
        .modal select,
        .modal input,
        .modal textarea {
            width: 100%;
            padding: 8px 10px;
            border: 1.5px solid var(--gray-300);
            border-radius: var(--radius-sm);
            font-family: var(--font-body);
            font-size: 0.83rem;
            background: var(--white);
            color: var(--gray-800);
            outline: none;
            transition: border-color var(--transition-fast);
        }
        .modal select:focus,
        .modal textarea:focus {
            border-color: var(--green-400);
            box-shadow: 0 0 0 3px rgba(61, 160, 102, 0.05);
        }
        .modal textarea {
            min-height: 55px;
            resize: vertical;
        }
        .modal .btn-row {
            display: flex;
            gap: 6px;
            margin-top: 14px;
            justify-content: flex-end;
        }
        .modal .checkbox-row {
            display: flex;
            align-items: center;
            gap: 6px;
            margin: 5px 0;
            cursor: pointer;
            font-size: 0.8rem;
            font-weight: 500;
            color: var(--gray-700);
            padding: 7px 11px;
            background: var(--gray-50);
            border-radius: var(--radius-sm);
            border: 1.5px dashed var(--gray-300);
            transition: all var(--transition-fast);
        }
        .modal .checkbox-row:hover {
            border-color: var(--purple-500);
            background: #f5f3ff;
        }
        .modal .checkbox-row input {
            width: 16px;
            height: 16px;
            cursor: pointer;
            accent-color: var(--purple-500);
        }
        .modal .checkbox-row.checked {
            border-color: var(--purple-500);
            background: #f5f3ff;
            border-style: solid;
        }
        .calendar-nav-row {
            display: flex;
            align-items: center;
            gap: 7px;
            margin-bottom: 10px;
            flex-wrap: wrap;
        }
        .month-title {
            font-weight: 700;
            font-size: 0.95rem;
            min-width: 130px;
            text-align: center;
            color: var(--gray-900);
        }
        .calendar-grid {
            display: grid;
            grid-template-columns: repeat(7, 1fr);
            gap: 3px;
            max-width: 580px;
            background: var(--white);
            border-radius: var(--radius-lg);
            padding: 8px;
            box-shadow: var(--shadow-sm);
            border: 1px solid var(--gray-200);
        }
        .day-hdr {
            text-align: center;
            font-weight: 700;
            font-size: 0.6rem;
            color: var(--gray-500);
            padding: 4px 0;
            letter-spacing: 0.04em;
            text-transform: uppercase;
        }
        .day-cell {
            aspect-ratio: 1;
            border-radius: var(--radius-sm);
            display: flex;
            align-items: center;
            justify-content: center;
            cursor: pointer;
            font-weight: 600;
            font-size: 0.76rem;
            border: 2px solid transparent;
            transition: all var(--transition-fast);
            position: relative;
        }
        .day-cell:hover {
            border-color: var(--green-300);
            background: var(--green-25);
            transform: scale(1.04);
        }
        .day-cell.today-ring {
            border-color: var(--green-500);
            box-shadow: 0 0 0 3px rgba(46, 139, 78, 0.1);
            font-weight: 800;
        }
        .day-cell.completed-day {
            background: #c8e6c9;
            color: #155724;
        }
        .day-cell.partial-day {
            background: #fff9c4;
            color: #5d4e00;
        }
        .day-cell.empty-day {
            background: var(--gray-100);
            color: var(--gray-600);
        }
        .day-cell.other-month {
            opacity: 0.25;
            pointer-events: none;
        }
        .calendar-legend {
            display: flex;
            gap: 12px;
            flex-wrap: wrap;
            margin-top: 8px;
            font-size: 0.64rem;
            color: var(--gray-500);
        }
        .legend-dot {
            width: 9px;
            height: 9px;
            border-radius: 2px;
            flex-shrink: 0;
        }
        .analytics-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(250px, 1fr));
            gap: 10px;
        }
        .stat-card {
            background: var(--white);
            border: 1px solid var(--gray-200);
            border-radius: var(--radius-lg);
            padding: 14px 16px;
            box-shadow: var(--shadow-sm);
            transition: box-shadow var(--transition);
        }
        .stat-card:hover {
            box-shadow: var(--shadow-md);
        }
        .stat-card h4 {
            font-size: 0.63rem;
            text-transform: uppercase;
            letter-spacing: 0.05em;
            color: var(--gray-500);
            margin-bottom: 4px;
            font-weight: 700;
        }
        .big-num {
            font-size: 1.8rem;
            font-weight: 800;
            color: var(--green-700);
            line-height: 1;
            letter-spacing: -0.02em;
        }
        .mini-bar-wrap {
            background: var(--gray-100);
            height: 6px;
            border-radius: 6px;
            margin-top: 3px;
            overflow: hidden;
        }
        .mini-bar-fill {
            height: 100%;
            background: linear-gradient(90deg, var(--green-400), var(--green-500));
            border-radius: 6px;
            transition: width 0.5s ease;
        }
        .coverage-list {
            list-style: none;
            font-size: 0.73rem;
            max-height: 260px;
            overflow-y: auto;
            line-height: 1.7;
        }
        .coverage-list li {
            padding: 2px 0;
            display: flex;
            gap: 3px;
            flex-wrap: wrap;
            border-bottom: 1px solid var(--gray-100);
        }
        .check {
            color: var(--green-500);
            font-weight: 700;
        }
        .cross {
            color: var(--red-500);
            font-weight: 700;
        }
        .pending-list {
            font-size: 0.76rem;
            line-height: 1.9;
            list-style: none;
            padding: 0;
        }
        .pending-list li {
            padding: 3px 0;
            border-bottom: 1px solid var(--gray-100);
            color: var(--gray-700);
        }
        .settings-card {
            background: var(--white);
            border: 1px solid var(--gray-200);
            border-radius: var(--radius-lg);
            padding: 18px 20px;
            box-shadow: var(--shadow-sm);
            max-width: 500px;
        }
        .settings-card input[type="text"] {
            width: 100%;
            padding: 8px 10px;
            border: 1.5px solid var(--gray-300);
            border-radius: var(--radius-sm);
            font-family: var(--font-body);
            font-size: 0.83rem;
            background: var(--white);
            color: var(--gray-800);
            outline: none;
            margin-bottom: 8px;
        }
        .toggle-row {
            display: flex;
            align-items: center;
            gap: 9px;
            margin: 7px 0;
            cursor: pointer;
            font-weight: 500;
            font-size: 0.82rem;
            color: var(--gray-700);
            user-select: none;
        }
        .toggle-switch {
            position: relative;
            display: inline-block;
            width: 42px;
            height: 22px;
            flex-shrink: 0;
        }
        .toggle-switch input {
            opacity: 0;
            width: 0;
            height: 0;
        }
        .toggle-slider {
            position: absolute;
            cursor: pointer;
            inset: 0;
            background: var(--gray-300);
            border-radius: 22px;
            transition: var(--transition-fast);
        }
        .toggle-slider::before {
            content: '';
            position: absolute;
            height: 16px;
            width: 16px;
            left: 3px;
            bottom: 3px;
            background: #fff;
            border-radius: 50%;
            transition: var(--transition-fast);
        }
        .toggle-switch input:checked+.toggle-slider {
            background: var(--green-500);
        }
        .toggle-switch input:checked+.toggle-slider::before {
            transform: translateX(20px);
        }
        .toast {
            position: fixed;
            bottom: 20px;
            right: 20px;
            z-index: 999;
            background: var(--green-800);
            color: #fff;
            padding: 9px 18px;
            border-radius: 20px;
            font-size: 0.78rem;
            font-weight: 600;
            box-shadow: var(--shadow-lg);
            transform: translateY(100px);
            opacity: 0;
            transition: all var(--transition-slow);
            pointer-events: none;
        }
        .toast.show {
            transform: translateY(0);
            opacity: 1;
        }
        .search-result-card {
            background: var(--white);
            border: 1px solid var(--gray-200);
            border-radius: var(--radius);
            padding: 10px 14px;
            box-shadow: var(--shadow-sm);
            transition: all var(--transition-fast);
        }
        .search-result-card:hover {
            box-shadow: var(--shadow-md);
        }
        .period-mapping-table {
            width: 100%;
            border-collapse: collapse;
            font-size: 0.72rem;
            margin-top: 4px;
        }
        .period-mapping-table th,
        .period-mapping-table td {
            border: 1px solid var(--gray-200);
            padding: 5px 7px;
            text-align: center;
        }
        .period-mapping-table th {
            background: var(--green-50);
            color: var(--green-800);
            font-weight: 700;
            font-size: 0.65rem;
            letter-spacing: 0.03em;
        }
        .period-mapping-table .done-row {
            background: #d4edda;
            color: #155724;
        }
        .period-mapping-table .pending-row {
            background: #fff9c4;
            color: #5d4e00;
        }
        ::-webkit-scrollbar {
            width: 4px;
        }
        ::-webkit-scrollbar-thumb {
            background: var(--gray-300);
            border-radius: 5px;
        }
        @media (max-width: 480px) {
            .btn {
                padding: 6px 10px;
                font-size: 0.68rem;
            }
            .period-card {
                padding: 10px 12px;
            }
            .class-title {
                font-size: 0.85rem;
            }
            .modal {
                padding: 14px 12px;
            }
            .analytics-grid {
                grid-template-columns: 1fr;
            }
        }
    </style>
</head>
<body>
    <header class="topbar">
        <button class="hamburger" id="hamburgerBtn" aria-label="Menu">&#9776;</button>
        <div class="topbar-logo">RPS</div>
        <div class="topbar-brand">
            <div class="topbar-school">R.P.S. School</div>
            <div class="topbar-sub">Practical Lesson Planner &middot; Bihar Sharif</div>
        </div>
        <div class="topbar-right">
            <span class="topbar-chip" id="topbarSession">Session 2026&ndash;2027</span>
            <span class="topbar-teacher"><span class="dot-live"></span> <span id="topbarTeacherName">Mr. Mithilesh</span></span>
            <span class="topbar-date" id="topbarDate"></span>
        </div>
    </header>

    <div class="app-shell">
        <div class="sidebar-overlay" id="sidebarOverlay"></div>
        <aside class="sidebar" id="sidebar">
            <div class="sidebar-header"><span class="session-badge">Bihar Sharif (Nalanda)</span></div>
            <nav class="sidebar-nav">
                <button data-view="today" class="active"><span class="nav-icon">&#128203;</span> Today's Plan</button>
                <button data-view="calendar"><span class="nav-icon">&#128197;</span> Calendar</button>
                <button data-view="analytics"><span class="nav-icon">&#128202;</span> Analytics</button>
                <button data-view="search"><span class="nav-icon">&#128269;</span> Search</button>
                <button data-view="settings"><span class="nav-icon">&#9881;</span> Settings</button>
            </nav>
            <div class="sidebar-footer">RPS Planner v3.0<br>Offline &middot; IndexedDB</div>
        </aside>

        <div class="main-area">
            <div class="content">
                <div class="print-only-header" id="printOnlyHeader">
                    <h1>R.P.S. School, Bihar Sharif (Nalanda)</h1>
                    <p>Session <span id="printSession"></span> | Practical Lesson Plan | Teacher: <span id="printOnlyTeacher">Mr. Mithilesh</span></p>
                    <p>Date: <span id="printOnlyDate"></span></p>
                </div>

                <!-- TODAY VIEW -->
                <div class="view active" id="view-today">
                    <div class="section-heading"><span class="accent-dot"></span><h2>Today's Schedule</h2><span class="date-chip" id="todayDateLabel"></span></div>
                    <div class="action-bar">
                        <button class="btn btn-gold" id="btnSmartSuggest">&#10024; Smart Suggest</button>
                        <button class="btn btn-primary" id="btnAutoFill">&#9889; Auto-Fill Empty</button>
                        <button class="btn btn-outline" id="btnPrintView">&#128424; Print</button>
                        <button class="btn btn-outline" id="btnShareWhatsApp">&#128241; Share WhatsApp</button>
                        <input type="text" class="search-input" id="searchClass" placeholder="Filter periods...">
                    </div>
                    <div class="suggestion-box" id="suggestionBox">
                        <div class="suggestion-title">&#128161; Smart Suggestions (period-matched)</div>
                        <div class="suggestion-chips" id="suggestionChips"><span class="suggestion-chip" style="opacity:0.6;">Loading...</span></div>
                    </div>
                    <div class="period-grid" id="periodGrid"></div>
                    <div class="print-period-table-wrap" id="printPeriodTableWrap">
                        <h3>Period-wise Distribution</h3>
                        <table class="print-period-table" id="printPeriodTable"><thead><tr><th>Period</th><th>Class</th><th>Section</th><th>Subject</th><th>Status</th><th>Notes</th></tr></thead><tbody id="printPeriodTbody"></tbody></table>
                        <div class="print-summary-box" id="printSummaryBox"></div>
                    </div>
                </div>

                <!-- CALENDAR VIEW -->
                <div class="view" id="view-calendar">
                    <div class="section-heading"><span class="accent-dot"></span><h2>Monthly Calendar</h2></div>
                    <div class="calendar-nav-row"><button class="btn btn-outline btn-sm" id="calPrev">&#9664;</button><span class="month-title" id="calMonthLabel"></span><button class="btn btn-outline btn-sm" id="calNext">&#9654;</button><button class="btn btn-ghost btn-sm" id="calGoToday">&#128205; Today</button></div>
                    <div class="calendar-grid" id="calendarGrid"></div>
                    <div class="calendar-legend"><span><span class="legend-dot" style="background:#c8e6c9;"></span> All Done</span><span><span class="legend-dot" style="background:#fff9c4;"></span> Partial</span><span><span class="legend-dot" style="background:#e5e7eb;"></span> Empty</span><span><span class="legend-dot" style="border:2px solid #2e8b4e;background:transparent;"></span> Today</span></div>
                </div>

                <!-- ANALYTICS VIEW -->
                <div class="view" id="view-analytics">
                    <div class="section-heading"><span class="accent-dot"></span><h2>Analytics Dashboard</h2></div>
                    <div class="analytics-grid" id="analyticsGrid"></div>
                    <div style="margin-top:14px;"><h4 style="margin-bottom:5px;color:var(--gray-500);font-size:0.66rem;text-transform:uppercase;letter-spacing:0.05em;">&#128204; Pending Work</h4><ul class="pending-list" id="pendingWorkList"></ul></div>
                </div>

                <!-- SEARCH VIEW -->
                <div class="view" id="view-search">
                    <div class="section-heading"><span class="accent-dot"></span><h2>Search Records</h2></div>
                    <div style="margin-bottom:10px;"><input type="text" class="search-input" id="globalSearchInput" placeholder="Search by class, subject, date, notes..." style="width:100%;max-width:460px;"></div>
                    <div id="searchResults" style="display:flex;flex-direction:column;gap:7px;"></div>
                </div>

                <!-- SETTINGS VIEW -->
                <div class="view" id="view-settings">
                    <div class="section-heading"><span class="accent-dot"></span><h2>Settings</h2></div>
                    <div class="settings-card">
                        <label>&#128100; Teacher Name</label><input type="text" id="settingTeacherName" value="Mr. Mithilesh">
                        <div class="toggle-row" id="darkModeToggleRow" style="margin-top:10px;"><span>&#127763; Dark Mode</span><label class="toggle-switch"><input type="checkbox" id="toggleDarkMode"><span class="toggle-slider"></span></label><span id="darkModeLabel" style="font-size:0.7rem;color:var(--gray-500);">Off</span></div>
                        <div style="margin-top:14px;display:flex;gap:6px;flex-wrap:wrap;">
                            <button class="btn btn-primary" id="btnSaveSettings">&#128190; Save</button>
                            <button class="btn btn-outline" id="btnExportData">&#128228; Export JSON</button>
                            <button class="btn btn-outline" id="btnImportData">&#128229; Import JSON</button>
                            <input type="file" id="importFileInput" accept=".json" hidden>
                            <button class="btn btn-danger" id="btnResetData">&#128465; Reset All</button>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- EDIT MODAL -->
    <div class="modal-overlay hidden" id="editModalOverlay">
        <div class="modal">
            <h3>&#9999; Edit Period</h3>
            <input type="hidden" id="editPeriodIndex"><input type="hidden" id="editExistingId"><input type="hidden" id="editPreviousStatus">
            <label class="checkbox-row" id="activityCheckboxRow"><input type="checkbox" id="editIsActivity"> &#128221; Mark as Activity / Duty</label>
            <div id="editActivityTypeSection" style="display:none;margin-top:4px;">
                <label>Activity Type</label>
                <select id="editActivityType">
                    <option value="Class Taken">&#128218; Class Taken</option>
                    <option value="Lab In-charge Duty">&#128300; Lab In-charge Duty</option>
                    <option value="Event Managed">&#127881; Event Managed</option>
                    <option value="Examination Duty">&#128221; Examination Duty</option>
                    <option value="Assembly Coordination">&#127908; Assembly Coordination</option>
                    <option value="Student Counseling">&#128172; Student Counseling</option>
                    <option value="Project Guidance">&#128736; Project Guidance</option>
                </select>
            </div>
            <div id="editClassSection" style="margin-top:4px;">
                <label>Class</label><select id="editClass"></select>
                <label>Section</label><select id="editSection"></select>
                <label>Subject</label><select id="editSubject"></select>
            </div>
            <label>Status</label>
            <select id="editStatus"><option value="not-started">&#9203; Not Started</option><option value="completed">&#9989; Completed</option><option value="skipped">&#9200; Skipped</option></select>
            <label id="editNotesLabel">Notes</label><textarea id="editNotes" placeholder="Optional notes..."></textarea>
            <div class="btn-row"><button class="btn btn-outline btn-sm" id="btnCancelEdit">Cancel</button><button class="btn btn-primary btn-sm" id="btnSaveEdit">&#128190; Save</button></div>
        </div>
    </div>
    <div class="toast" id="toast"></div>

    <script>
        (() => {
            const DB_NAME = 'RPS_Planner_DB';
            const DB_VERSION = 4;
            const CLASSES = ['VI', 'VII', 'VIII', 'IX', 'X', 'XI', 'XII'];
            const SECTIONS_MAP = { VI: ['A', 'B', 'C'], VII: ['A', 'B', 'C', 'D'], VIII: ['A', 'B', 'C', 'D'], IX: ['A',
                    'B', 'C', 'D', 'E'
                ], X: ['A', 'B', 'C', 'D', 'E'], XI: ['A', 'B', 'C'], XII: ['A', 'B', 'C'] };
            const SUBJECTS = ['Physics', 'Chemistry', 'Biology'];
            const PERIODS = [1, 2, 3, 4, 5, 6, 7];
            const TARGET_PER_SECTION = 10;

            // ── PERIOD MAPPING (class-section → period) ──
            const PERIOD_MAPPING = {
                'VI': { 'A': 1, 'B': 3, 'C': 2 },
                'VII': { 'A': 4, 'B': 5, 'C': 3, 'D': 2 },
                'VIII': { 'A': 4, 'B': 5, 'C': 1, 'D': 6 },
                'IX': { 'A': 3, 'B': 4, 'C': 7, 'D': 6, 'E': 1 },
                'X': { 'A': 5, 'B': 1, 'C': 4, 'D': 2, 'E': 6 },
                'XI': {},
                'XII': { 'A': { 'Physics': 2, 'Chemistry': 3, 'Biology': 5 }, 'B': {}, 'C': {} }
            };

            const ACTIVITY_TYPES = ['Class Taken', 'Lab In-charge Duty', 'Event Managed', 'Examination Duty',
                'Assembly Coordination', 'Student Counseling', 'Project Guidance'
            ];

            const CLASS_ORDER = Object.assign({}, ...CLASSES.map((c, i) => ({ [c]: i })));
            const TODAY = new Date();
            const ACADEMIC_YEAR_START = TODAY.getMonth() >= 3 ? TODAY.getFullYear() : TODAY.getFullYear() - 1;
            const SESSION_STRING = `${ACADEMIC_YEAR_START}–${ACADEMIC_YEAR_START + 1}`;

            const dateStr = d => { const y = d.getFullYear(),
                    m = String(d.getMonth() + 1).padStart(2, '0'),
                    day = String(d.getDate()).padStart(2, '0'); return `${y}-${m}-${day}`; };
            const parseDateSafe = s => { if (!s || typeof s !== 'string') return null; const p = s.split('-'); if (p
                    .length !== 3) return null; const [y, m, d] = p.map(Number); if (isNaN(y) || isNaN(m) || isNaN(d))
                    return null; const dt = new Date(y, m - 1, d); return isNaN(dt.getTime()) ? null : dt; };

            let currentView = 'today',
                currentDate = dateStr(TODAY),
                calMonth = TODAY.getMonth(),
                calYear = TODAY.getFullYear(),
                db, darkMode = false;

            function openDB() {
                return new Promise((res, rej) => {
                    const req = indexedDB.open(DB_NAME, DB_VERSION);
                    req.onupgradeneeded = e => {
                        const d = e.target.result;
                        if (!d.objectStoreNames.contains('progress')) d.createObjectStore('progress', { keyPath: 'id' });
                        if (!d.objectStoreNames.contains('schedule')) { const s = d.createObjectStore('schedule', { keyPath: 'id',
                                autoIncrement: true }); s.createIndex('date', 'date', { unique: false }); s.createIndex(
                                'date_period', ['date', 'period'], { unique: true }); }
                        if (!d.objectStoreNames.contains('settings')) d.createObjectStore('settings', { keyPath: 'key' });
                    };
                    req.onsuccess = e => { db = e.target.result;
                        res(db); };
                    req.onerror = e => rej(e.target.error);
                });
            }
            const dbPut = (store, data) => new Promise((res, rej) => { const tx = db.transaction(store, 'readwrite');
                tx.objectStore(store).put(data);
                tx.oncomplete = () => res();
                tx.onerror = e => rej(e.target.error); });
            const dbGetAll = store => new Promise((res, rej) => { const tx = db.transaction(store, 'readonly'); const req = tx
                    .objectStore(store).getAll();
                req.onsuccess = () => res(req.result);
                req.onerror = e => rej(e.target.error); });
            const dbGet = (store, key) => new Promise((res, rej) => { const tx = db.transaction(store, 'readonly'); const req = tx
                    .objectStore(store).get(key);
                req.onsuccess = () => res(req.result);
                req.onerror = e => rej(e.target.error); });
            const dbDelete = (store, key) => new Promise((res, rej) => { const tx = db.transaction(store, 'readwrite');
                tx.objectStore(store).delete(key);
                tx.oncomplete = () => res();
                tx.onerror = e => rej(e.target.error); });
            const dbClear = store => new Promise((res, rej) => { const tx = db.transaction(store, 'readwrite');
                tx.objectStore(store).clear();
                tx.oncomplete = () => res();
                tx.onerror = e => rej(e.target.error); });

            async function getScheduleForDate(date) { const all = await dbGetAll('schedule'); const map = {};
                PERIODS.forEach(p => map[p] = null);
                all.filter(s => s.date === date).forEach(s => { map[s.period] = s; }); return map; }

            function getProgressKey(cls, section, subject) {
                if (['VI', 'VII', 'VIII', 'IX', 'X'].includes(cls)) return `${cls}_${section}`;
                return `${cls}_${section}_${subject}`;
            }

            function getSubjectForClass(cls) { return ['VI', 'VII', 'VIII', 'IX', 'X'].includes(cls) ? 'Science' : null; }

            async function initProgress() {
                const existing = await dbGetAll('progress');
                if (existing.length > 0) return;
                for (const cls of CLASSES) {
                    const sections = SECTIONS_MAP[cls] || [];
                    for (const section of sections) {
                        if (['VI', 'VII', 'VIII', 'IX', 'X'].includes(cls)) {
                            await dbPut('progress', { id: getProgressKey(cls, section, 'Science'), class: cls, section,
                                subject: 'Science', completedCount: 0, target: TARGET_PER_SECTION });
                        } else {
                            for (const subj of SUBJECTS) {
                                await dbPut('progress', { id: getProgressKey(cls, section, subj), class: cls, section,
                                    subject: subj, completedCount: 0, target: TARGET_PER_SECTION });
                            }
                        }
                    }
                }
            }
            async function initSettings() {
                let s = await dbGet('settings', 'app'); if (!s) { s = { key: 'app', teacherName: 'Mr. Mithilesh',
                        darkMode: false }; await dbPut('settings', s); }
                darkMode = s.darkMode;
                applyDarkMode(darkMode);
                document.getElementById('settingTeacherName').value = s.teacherName;
                document.getElementById('toggleDarkMode').checked = darkMode;
                document.getElementById('darkModeLabel').textContent = darkMode ? 'On' : 'Off';
                document.getElementById('topbarTeacherName').textContent = s.teacherName;
                document.getElementById('printOnlyTeacher').textContent = s.teacherName;
                document.getElementById('topbarSession').textContent = 'Session ' + SESSION_STRING;
                document.getElementById('printSession').textContent = SESSION_STRING;
            }

            function applyDarkMode(enable) { darkMode = enable;
                document.body.classList.toggle('dark-mode', enable);
                document.getElementById('darkModeLabel').textContent = enable ? 'On' : 'Off';
                document.getElementById('toggleDarkMode').checked = enable; }

            function getDesignatedPeriod(cls, section, subject) {
                if (['VI', 'VII', 'VIII', 'IX', 'X'].includes(cls)) { const m = PERIOD_MAPPING[cls]; return m && m[section] ?
                        m[section] : null; } if (cls === 'XII') { const m = PERIOD_MAPPING['XII']; if (m && m[section] && typeof m[
                            section] === 'object') return m[section][subject] || null; return null; } return null;
            }

            function getSectionSubjectLabel(cls, section, subject) { if (['VI', 'VII', 'VIII', 'IX', 'X'].includes(cls))
                    return `${cls}${section} Science`; return `${cls}${section} ${subject}`; }

            async function getAllSectionProgress() {
                const all = await dbGetAll('progress');
                const map = {};
                all.forEach(p => { map[p.id] = p; });
                return map;
            }

            async function getSmartSuggestions() {
                const progressMap = await getAllSectionProgress();
                const candidates = [];
                for (const cls of CLASSES) { const sections = SECTIONS_MAP[cls] || []; for (const section of sections) { if ([
                            'VI', 'VII', 'VIII', 'IX', 'X'
                        ].includes(cls)) { const key = getProgressKey(cls, section, 'Science');
                        const prog = progressMap[key]; const period = getDesignatedPeriod(cls, section, 'Science'); if (prog &&
                            prog.completedCount < prog.target && period) { candidates.push({ cls, section,
                                subject: 'Science', period, completed: prog.completedCount, target: prog.target,
                                key }); } } else { for (const subj of SUBJECTS) { const key = getProgressKey(cls, section,
                                subj); const prog = progressMap[key]; const period = getDesignatedPeriod(cls,
                            section, subj); if (prog && prog.completedCount < prog.target && period) { candidates.push({ cls,
                                section, subject: subj, period, completed: prog.completedCount, target: prog
                                .target, key }); } } } } }
                candidates.sort((a, b) => a.completed - b.completed || (CLASS_ORDER[a.cls] - CLASS_ORDER[b.cls]) || (a.section
                    .localeCompare(b.section)));
                const periodMap = {};
                PERIODS.forEach(p => periodMap[p] = null);
                const usedKeys = new Set();
                for (const c of candidates) { if (!periodMap[c.period] && !usedKeys.has(c.key)) { periodMap[c.period] = c;
                        usedKeys.add(c.key); } }
                for (const c of candidates) { if (usedKeys.has(c.key)) continue; for (const p of PERIODS) { if (!periodMap[p]) {
                            periodMap[p] = c;
                            usedKeys.add(c.key); break; } } }
                const result = [];
                for (const p of PERIODS) { if (periodMap[p]) result.push({ period: p, ...periodMap[p] }); }
                return result;
            }

            async function renderSuggestions() {
                const suggestions = await getSmartSuggestions();
                const container = document.getElementById('suggestionChips');
                if (!suggestions.length) { container.innerHTML =
                        '<span class="suggestion-chip" style="opacity:0.6;">All sections on track! &#127881;</span>'; return; }
                container.innerHTML = suggestions.map((s, i) =>
                    `<span class="suggestion-chip" title="Period ${s.period} | ${s.completed}/${s.target} done" data-idx="${i}">P${s.period}: ${s.cls}${s.section} ${s.subject} (${s.completed}/10)</span>`
                ).join('');
                container.querySelectorAll('.suggestion-chip').forEach(chip => { chip.addEventListener('click', async () => {
                        const idx = +chip.dataset.idx; const s = suggestions[idx]; if (!s) return;
                        const sched = await getScheduleForDate(currentDate); const existing = sched[s
                        .period]; if (existing && existing.class && existing.status !== 'not-started') { toast(
                                `Period ${s.period} already has a class in progress!`); return; } if (
                            existing) await dbDelete('schedule', existing.id); await dbPut('schedule', { date: currentDate,
                            period: s.period, class: s.cls, section: s.section, subject: s.subject,
                            status: 'not-started', notes: '', isActivity: false, activityType: '' });
                        await renderToday(currentDate); if (currentView === 'calendar') renderCalendar();
                        toast(`Period ${s.period} set: ${s.cls}${s.section} ${s.subject}`); }); });
            }

            async function renderToday(date) {
                currentDate = date;
                document.getElementById('todayDateLabel').textContent = formatDateNice(date);
                document.getElementById('topbarDate').textContent = formatDateShort(date);
                document.getElementById('printOnlyDate').textContent = formatDateNice(date);
                const schedule = await getScheduleForDate(date);
                const grid = document.getElementById('periodGrid');
                const search = (document.getElementById('searchClass')?.value || '').trim().toUpperCase();
                await renderSuggestions();
                let html = '';
                const printTbody = document.getElementById('printPeriodTbody');
                let printTableHtml = '',
                    totalP = 0,
                    doneC = 0,
                    skipC = 0,
                    pendC = 0,
                    actC = 0;
                for (const p of PERIODS) {
                    const e = schedule[p]; if (search && e && !`${e.class||''} ${e.section||''} ${e.subject||''} ${e.notes||''} ${e.activityType||''}`.toUpperCase()
                        .includes(search)) continue; if (search && !e) continue;
                    totalP++; const sfp = e ? e.status : 'not-started'; if (sfp === 'completed') doneC++; else if (sfp ===
                        'skipped') skipC++; else pendC++; if (e && e.isActivity) actC++;
                    const psl = !e ? 'Not Started' : e.status === 'completed' ? 'Completed' : e.status === 'skipped' ?
                        'Skipped' : 'Not Started';
                    const pCls = e && !e.isActivity ? e.class : (e && e.isActivity ? (e.activityType || 'Activity') :
                        '—');
                    const pSec = e && !e.isActivity ? e.section : (e && e.isActivity ? '—' : '—');
                    const pSub = e && !e.isActivity ? e.subject : (e && e.isActivity ? '—' : '—');
                    const pNt = e ? (e.notes || '—') : '—';
                    printTableHtml +=
                        `<tr><td>${p}</td><td>${pCls}</td><td>${pSec}</td><td>${pSub}</td><td>${psl}</td><td>${pNt}</td></tr>`;
                    if (!e) { html +=
                            `<div class="period-card" data-period="${p}"><span class="period-chip">Period ${p}</span><div class="class-title" style="color:#999;">— No class scheduled —</div><div class="card-actions-row"><button class="btn btn-outline btn-sm btn-edit-period" data-period="${p}">✏️ Edit</button></div></div>`;
                        continue; }
                    let cc = '',
                        sb = '',
                        sc = 'status-not-started'; if (e.status === 'completed') { cc = 'completed';
                        sb = 'Completed';
                        sc = 'status-completed'; } else if (e.status === 'skipped') { cc = 'skipped';
                        sb = 'Skipped';
                        sc = 'status-skipped'; } else sb = 'Not Started';
                    const isA = e.isActivity === true; if (isA && !cc) cc = 'activity-card';
                    const title = isA ? (e.activityType || 'Activity') :
                        `<strong>Class ${e.class} — Section ${e.section}</strong>`;
                    const aTag = isA ? '<span class="activity-badge-inline">ACTIVITY</span>' : '';
                    const subj = isA ? '' : `<div class="subject-tag">Subject: ${e.subject}</div>`;
                    const notes = e.notes ? `<div class="notes-snippet">Notes: ${e.notes}</div>` : '';
                    html +=
                        `<div class="period-card ${cc}" data-period="${p}"><span class="period-chip">Period ${p}</span><div class="class-title">${title}${aTag}</div>${subj}<span class="status-pill ${sc}">${sb}</span>${notes}<div class="card-actions-row"><button class="btn btn-outline btn-sm btn-edit-period" data-period="${p}">✏️ Edit</button><button class="btn btn-success btn-sm btn-mark-done" data-period="${p}">Done</button><button class="btn btn-warn btn-sm btn-mark-skip" data-period="${p}">Skip</button>${isA&&e.notes?`<button class="btn btn-outline btn-sm btn-share-note" data-period="${p}">Share</button>`:''}</div></div>`;
                }
                grid.innerHTML = html ||
                    '<p style="text-align:center;padding:20px;color:var(--gray-400);">No matching periods.</p>';
                printTbody.innerHTML = printTableHtml ||
                    '<tr><td colspan="6" style="text-align:center;">No schedule data.</td></tr>';
                document.getElementById('printSummaryBox').innerHTML = `
              <strong>Summary</strong> <div style="display:flex;gap:16px;flex-wrap:wrap;">
              <span>Total: <strong>${totalP}</strong></span> <span>Done: <strong>${doneC}</strong></span>
              <span>Skipped: <strong>${skipC}</strong></span> <span>Pending: <strong>${pendC}</strong></span>
              <span>Activities: <strong>${actC}</strong></span></div>`;
                grid.querySelectorAll('.btn-edit-period').forEach(b => b.addEventListener('click', () => openEditModal(+b
                    .dataset.period)));
                grid.querySelectorAll('.btn-mark-done').forEach(b => b.addEventListener('click', async () => { await
                        quickUpdate(+b.dataset.period, 'completed');
                    renderToday(currentDate); if (currentView === 'calendar') renderCalendar(); }));
                grid.querySelectorAll('.btn-mark-skip').forEach(b => b.addEventListener('click', async () => { await
                        quickUpdate(+b.dataset.period, 'skipped');
                    renderToday(currentDate); if (currentView === 'calendar') renderCalendar(); }));
                grid.querySelectorAll('.btn-share-note').forEach(b => b.addEventListener('click', () => shareActivityNote(+
                    b.dataset.period)));
            }

            async function quickUpdate(period, status) { const sched = await getScheduleForDate(currentDate); const entry =
                    sched[period]; if (!entry) return; const prevStatus = entry.status;
                entry.status = status; await dbPut('schedule', entry); if (status === 'completed' && prevStatus !==
                    'completed' && !entry.isActivity && entry.class && entry.section && entry.subject) { await
                        incrementProgress(entry.class, entry.section, entry.subject); }
                toast(status === 'completed' ? 'Marked completed ✓' : 'Marked skipped'); }

            async function incrementProgress(cls, section, subject) {
                const subj = ['VI', 'VII', 'VIII', 'IX', 'X'].includes(cls) ? 'Science' : subject;
                const key = getProgressKey(cls, section, subj);
                const prog = await dbGet('progress', key); if (!prog) return; if (prog.completedCount < prog.target) { prog
                        .completedCount++; await dbPut('progress', prog); }
            }
            async function decrementProgress(cls, section, subject) {
                const subj = ['VI', 'VII', 'VIII', 'IX', 'X'].includes(cls) ? 'Science' : subject;
                const key = getProgressKey(cls, section, subj);
                const prog = await dbGet('progress', key); if (!prog) return; if (prog.completedCount > 0) { prog.completedCount--;
                    await dbPut('progress', prog); }
            }

            async function shareActivityNote(period) { const sched = await getScheduleForDate(currentDate); const e = sched[
                    period]; if (!e || !e.isActivity || !e.notes) return;
                const msg =
                    `*Activity Report*\nSchool: R.P.S. School, Bihar Sharif\nTeacher: ${document.getElementById('topbarTeacherName').textContent}\nDate: ${formatDateNice(currentDate)}\nPeriod: ${period}\nType: ${e.activityType||'Activity'}\nNotes: ${e.notes}`;
                window.open(`https://wa.me/?text=${encodeURIComponent(msg)}`, '_blank'); }

            let curEditId = null,
                curEditPrevStatus = 'not-started';

            async function openEditModal(period) { const sched = await getScheduleForDate(currentDate); const entry = sched[
                    period];
                document.getElementById('editPeriodIndex').value = period; if (entry) { curEditId = entry.id || null;
                    curEditPrevStatus = entry.status || 'not-started';
                    document.getElementById('editExistingId').value = entry.id || '';
                    document.getElementById('editPreviousStatus').value = entry.status || 'not-started'; } else { curEditId =
                        null;
                    curEditPrevStatus = 'not-started';
                    document.getElementById('editExistingId').value = '';
                    document.getElementById('editPreviousStatus').value = 'not-started'; } const isActivity = entry ? (entry
                    .isActivity || false) : false;
                document.getElementById('editIsActivity').checked = isActivity;
                toggleActivityUI(isActivity); if (isActivity && entry) { document.getElementById('editActivityType').value =
                        entry.activityType || 'Class Taken'; }
                document.getElementById('editStatus').value = (entry && entry.status) ? entry.status : 'not-started';
                document.getElementById('editNotes').value = (entry && entry.notes) ? entry.notes : ''; if (!isActivity) { const
                        classSel = document.getElementById('editClass');
                    classSel.innerHTML = CLASSES.map(c =>
                        `<option value="${c}" ${entry&&c===entry.class?'selected':''}>Class ${c}</option>`).join('');
                    classSel.value = (entry && entry.class) ? entry.class : 'VI';
                    updateSectionDropdown(classSel.value, entry ? entry.section : 'A');
                    document.getElementById('editSection').value = (entry && entry.section) ? entry.section : 'A';
                    const subjSel = document.getElementById('editSubject');
                    subjSel.innerHTML = SUBJECTS.map(s =>
                        `<option value="${s}" ${entry&&s===entry.subject?'selected':''}>${s}</option>`).join('');
                    subjSel.value = (entry && entry.subject && SUBJECTS.includes(entry.subject)) ? entry.subject :
                    'Physics';
                    classSel.onchange = function() { updateSectionDropdown(this.value); }; }
                document.getElementById('editModalOverlay').classList.remove('hidden'); }

            function toggleActivityUI(isA) {
                document.getElementById('editClassSection').style.display = isA ? 'none' : 'block';
                document.getElementById('editActivityTypeSection').style.display = isA ? 'block' : 'none';
                document.getElementById('editNotesLabel').textContent = isA ? 'Activity Notes' : 'Notes';
                document.getElementById('activityCheckboxRow').classList.toggle('checked', isA);
            }
            document.getElementById('editIsActivity').addEventListener('change', function() { toggleActivityUI(this
            .checked); });

            function updateSectionDropdown(cls, selected) { const sel = document.getElementById('editSection'); const secs =
                    SECTIONS_MAP[cls] || ['A'];
                sel.innerHTML = secs.map(s =>
                    `<option value="${s}" ${s===selected?'selected':''}>Section ${s}</option>`).join(''); if (selected && secs
                    .includes(selected)) sel.value = selected; }
            document.getElementById('btnCancelEdit').addEventListener('click', () => { document.getElementById(
                'editModalOverlay').classList.add('hidden');
                curEditId = null;
                curEditPrevStatus = 'not-started'; });
            document.getElementById('btnSaveEdit').addEventListener('click', async () => { const isActivity = document
                    .getElementById('editIsActivity').checked; const period = +document.getElementById('editPeriodIndex')
                    .value; const status = document.getElementById('editStatus').value; const notes = document.getElementById(
                        'editNotes').value.trim(); const prevStatus = curEditPrevStatus; const activityType = isActivity ?
                    document.getElementById('editActivityType').value : ''; let entry; if (isActivity) { entry = { date: currentDate,
                        period, class: '', section: '', subject: '', status, notes, isActivity: true,
                        activityType }; } else { const cls = document.getElementById('editClass').value; const sec = document
                        .getElementById('editSection').value; const subj = document.getElementById('editSubject').value;
                    entry = { date: currentDate, period, class: cls, section: sec, subject: subj, status, notes,
                        isActivity: false, activityType: '' }; } if (curEditId !== null && curEditId !== undefined &&
                    curEditId !== '') { entry.id = typeof curEditId === 'number' ? curEditId : parseInt(curEditId,
                    10); } try { await dbPut('schedule', entry); if (status === 'completed' && prevStatus !==
                        'completed' && !isActivity && entry.class && entry.section && entry.subject) { await
                            incrementProgress(entry.class, entry.section, entry.subject); } else if (status !==
                        'completed' && prevStatus === 'completed' && !isActivity && entry.class && entry.section &&
                        entry.subject) { await decrementProgress(entry.class, entry.section, entry.subject); }
                    document.getElementById('editModalOverlay').classList.add('hidden');
                    curEditId = null;
                    curEditPrevStatus = 'not-started'; await renderToday(currentDate); if (currentView === 'calendar')
                        renderCalendar(); if (currentView === 'analytics') renderAnalytics();
                    toast('Period saved! 💾'); } catch (err) { console.error('Save failed:', err);
                    toast('Save failed!'); } });
            document.getElementById('editModalOverlay').addEventListener('click', e => { if (e.target === e.currentTarget) { e
                    .target.classList.add('hidden');
                    curEditId = null;
                    curEditPrevStatus = 'not-started'; } });

            document.getElementById('btnSmartSuggest').addEventListener('click', async () => { const suggestions = await
                    getSmartSuggestions(); const sched = await getScheduleForDate(currentDate); for (const p of PERIODS) { if (
                        sched[p]) await dbDelete('schedule', sched[p].id); } for (const s of suggestions) { await dbPut(
                        'schedule', { date: currentDate, period: s.period, class: s.cls, section: s.section, subject: s
                            .subject, status: 'not-started', notes: '', isActivity: false, activityType: '' }); } await
                    renderToday(currentDate); if (currentView === 'calendar') renderCalendar();
                toast('Smart suggestions applied! ✨'); });
            document.getElementById('btnAutoFill').addEventListener('click', async () => { const suggestions = await
                    getSmartSuggestions(); const sched = await getScheduleForDate(currentDate); const filled = new Set(
                        PERIODS.filter(p => sched[p] && sched[p].class)); let count = 0; for (const s of suggestions) { if (
                        filled.has(s.period)) continue; await dbPut('schedule', { date: currentDate, period: s.period,
                        class: s.cls, section: s.section, subject: s.subject, status: 'not-started', notes: '',
                        isActivity: false, activityType: '' });
                    filled.add(s.period);
                    count++; } await renderToday(currentDate); if (currentView === 'calendar') renderCalendar();
                toast(`Filled ${count} empty slots! ⚡`); });
            document.getElementById('searchClass').addEventListener('input', () => renderToday(currentDate));
            document.getElementById('btnPrintView').addEventListener('click', () => { renderToday(currentDate).then(() =>
                window.print()); });
            document.getElementById('btnShareWhatsApp').addEventListener('click', async () => { const sched = await
                    getScheduleForDate(currentDate); const tName = document.getElementById('topbarTeacherName')
                    .textContent; let msg =
                    `*R.P.S. School, Bihar Sharif*\nSession ${SESSION_STRING} | Teacher: ${tName}\nDate: ${formatDateNice(currentDate)}\n${'='.repeat(30)}\n\n`;
                let hd = false; for (const p of PERIODS) { const e = sched[p]; if (!e) continue;
                    hd = true; const lbl = e.isActivity ? `[${e.activityType||'Activity'}]` :
                        `Class ${e.class}-${e.section} | ${e.subject}`; const si = e.status === 'completed' ? '[Done]' :
                        e.status === 'skipped' ? '[Skip]' : '[Pend]';
                    msg += `P${p}: ${lbl} ${si}${e.notes?' | '+e.notes:''}\n`; } if (!hd) msg +=
                    '(No schedule)\n';
                msg += `\n${'='.repeat(30)}\n`; const tot = PERIODS.filter(p => sched[p]).length; const dn = PERIODS
                    .filter(p => sched[p] && sched[p].status === 'completed').length;
                msg += `Summary: ${tot} periods | Done: ${dn} | Skip: ${PERIODS.filter(p=>sched[p]&&sched[p].status==='skipped').length} | Pend: ${tot-dn-PERIODS.filter(p=>sched[p]&&sched[p].status==='skipped').length}\n`;
                window.open(`https://wa.me/?text=${encodeURIComponent(msg)}`, '_blank'); });

            async function renderCalendar() {
                document.getElementById('calMonthLabel').textContent = new Date(calYear, calMonth).toLocaleString(
                    'default', { month: 'long', year: 'numeric' });
                const grid = document.getElementById('calendarGrid');
                const days = ['Sun', 'Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat']; let html = days.map(d =>
                    `<div class="day-hdr">${d}</div>`).join('');
                const fd = new Date(calYear, calMonth, 1).getDay(),
                    dim = new Date(calYear, calMonth + 1, 0).getDate();
                const allS = await dbGetAll('schedule');
                const dm = {};
                allS.forEach(s => { if (!dm[s.date]) dm[s.date] = { total: 0, completed: 0 };
                    dm[s.date].total++; if (s.status === 'completed') dm[s.date].completed++; });
                const gc = (d, m, y) => { const ds = dateStr(new Date(y, m, d)); let c = 'day-cell'; if (ds === dateStr(
                        TODAY)) c += ' today-ring'; const inf = dm[ds]; if (inf && inf.total > 0) { c += inf.completed >=
                        inf.total ? ' completed-day' : (inf.completed > 0 ? ' partial-day' : ' empty-day'); } else c +=
                        ' empty-day'; return c; };
                for (let i = fd - 1; i >= 0; i--) { const d = new Date(calYear, calMonth, 0).getDate() - i;
                    html += `<div class="day-cell other-month">${d}</div>`; } for (let d = 1; d <= dim; d++) { html +=
                        `<div class="${gc(d,calMonth,calYear)}" data-date="${dateStr(new Date(calYear,calMonth,d))}">${d}</div>`; }
                const tc = fd + dim,
                    rem = tc % 7 === 0 ? 0 : 7 - (tc % 7); for (let d = 1; d <= rem; d++) { const m = calMonth === 11 ? 0 :
                        calMonth + 1,
                    y = calMonth === 11 ? calYear + 1 : calYear;
                    html += `<div class="day-cell other-month">${d}</div>`; }
                grid.innerHTML = html;
                grid.querySelectorAll('.day-cell:not(.other-month)').forEach(cell => { cell.addEventListener('click',
                    async () => { if (cell.dataset.date) { currentDate = cell.dataset.date; await renderToday(
                            currentDate);
                        switchView('today'); } }); });
            }
            document.getElementById('calPrev').onclick = () => { calMonth--; if (calMonth < 0) { calMonth = 11;
                    calYear--; } renderCalendar(); };
            document.getElementById('calNext').onclick = () => { calMonth++; if (calMonth > 11) { calMonth = 0;
                    calYear++; } renderCalendar(); };
            document.getElementById('calGoToday').onclick = () => { calMonth = TODAY.getMonth();
                calYear = TODAY.getFullYear();
                currentDate = dateStr(TODAY);
                renderCalendar();
                renderToday(currentDate);
                switchView('today'); };

            async function renderAnalytics() {
                const allS = await dbGetAll('schedule');
                const progMap = await getAllSectionProgress();
                const completed = allS.filter(s => s.status === 'completed' && !s.isActivity);
                const totalComp = completed.length;
                let totalTarget = 0,
                    totalDoneAll = 0; for (const k in progMap) { totalTarget += progMap[k].target;
                    totalDoneAll += progMap[k].completedCount; }
                const subjCounts = {};
                SUBJECTS.forEach(s => subjCounts[s] = 0);
                completed.forEach(s => { if (SUBJECTS.includes(s.subject)) subjCounts[s.subject]++; });
                const periodUse = {};
                PERIODS.forEach(p => periodUse[p] = 0);
                allS.forEach(s => periodUse[s.period]++);
                const maxP = Math.max(1, ...Object.values(periodUse));
                let periodMappingHTML =
                    '<table class="period-mapping-table"><thead><tr><th>Period</th><th>Assigned Class-Sections</th><th>Status</th></tr></thead><tbody>';
                for (const p of PERIODS) { const assigned = []; for (const cls of CLASSES) { const secs = SECTIONS_MAP[cls] ||
                            []; for (const sec of secs) { if (['VI', 'VII', 'VIII', 'IX', 'X'].includes(cls)) { if (
                                getDesignatedPeriod(cls, sec, 'Science') === p) { const pk = getProgressKey(cls, sec,
                                    'Science'); const pr = progMap[pk];
                                assigned.push({ label: `${cls}${sec} Science`, done: pr ? pr.completedCount : 0,
                                    target: TARGET_PER_SECTION }); } } else { for (const subj of SUBJECTS) { if (
                                        getDesignatedPeriod(cls, sec, subj) === p) { const pk = getProgressKey(cls,
                                        sec, subj); const pr = progMap[pk];
                                    assigned.push({ label: `${cls}${sec} ${subj}`, done: pr ? pr.completedCount :
                                        0, target: TARGET_PER_SECTION }); } } } } } const aHTML = assigned.length ?
                        assigned.map(a => {
                            const pct = Math.round(a.done / a.target * 100); const rowCls = a.done >= a.target ?
                                'done-row' : 'pending-row'; return `<span class="${rowCls}" style="display:inline-block;padding:2px 6px;margin:2px;border-radius:4px;font-size:0.68rem;">${a.label}: ${a.done}/10</span>`;
                        }).join(' ') : '<span style="color:#999;">— Flexible —</span>';
                    periodMappingHTML +=
                        `<tr><td><strong>Period ${p}</strong></td><td>${aHTML}</td><td>${assigned.length?assigned.every(a=>a.done>=a.target)?'✅ All Done':'🔄 In Progress':'—'}</td></tr>`; }
                periodMappingHTML += '</tbody></table>';
                const grid = document.getElementById('analyticsGrid');
                grid.innerHTML = `
            <div class="stat-card"><h4>Total Completed</h4><div class="big-num">${totalComp}</div></div>
            <div class="stat-card"><h4>Overall Progress</h4><div class="big-num">${totalDoneAll} / ${totalTarget}</div><div class="mini-bar-wrap"><div class="mini-bar-fill" style="width:${totalTarget?Math.round(totalDoneAll/totalTarget*100):0}%"></div></div></div>
            <div class="stat-card"><h4>By Subject</h4>${SUBJECTS.map(s=>`<div style="margin:2px 0;">${s}: <strong>${subjCounts[s]||0}</strong></div>`).join('')}</div>
            <div class="stat-card"><h4>Period Usage</h4>${PERIODS.map(p=>`<div style="margin:1px 0;">P${p}: <strong>${periodUse[p]}</strong><div class="mini-bar-wrap"><div class="mini-bar-fill" style="width:${Math.round(periodUse[p]/maxP*100)}%"></div></div></div>`).join('')}</div>
            <div class="stat-card" style="grid-column:1/-1;"><h4>📋 Period-wise Science Class Mapping</h4>${periodMappingHTML}</div>`;
                let pendingHTML = ''; for (const k in progMap) { const pr = progMap[k]; if (pr.completedCount < pr.target) {
                        pendingHTML +=
                            `<li>📌 <strong>${pr.class}${pr.section}</strong> — ${pr.subject}: ${pr.completedCount}/${pr.target} done (need ${pr.target-pr.completedCount} more)</li>`; } }
                document.getElementById('pendingWorkList').innerHTML = pendingHTML ||
                    '<li>✅ All sections covered — great job!</li>';
            }

            document.getElementById('globalSearchInput').addEventListener('input', async function() { const q = this.value
                    .trim().toLowerCase(); const container = document.getElementById('searchResults'); if (!q) { container
                        .innerHTML =
                        '<p style="color:var(--gray-400);text-align:center;padding:16px;">Type to search records.</p>'; return; }
                const allS = await dbGetAll('schedule'); const results = allS.filter(r =>
                    `${r.class||''} ${r.section||''} ${r.subject||''} ${r.date||''} ${r.notes||''} ${r.activityType||''}`.toLowerCase()
                    .includes(q)).sort((a, b) => (b.date || '').localeCompare(a.date || '')); if (!results.length) { container
                        .innerHTML =
                        '<p style="color:var(--gray-400);text-align:center;padding:16px;">No records found.</p>'; return; }
                container.innerHTML = results.slice(0, 60).map(r => { const dt = parseDateSafe(r.date); const dd = dt ?
                        formatDateShort(dt) : (r.date || 'Unknown'); return `
            <div class="search-result-card"><div class="search-result-top"><span class="period-chip">P${r.period}</span><strong>${r.isActivity?(r.activityType||'Activity'):`Class ${r.class} — ${r.section}`}</strong>${!r.isActivity?`<span style="color:var(--gray-500);font-size:0.74rem;">${r.subject}</span>`:''}<span class="status-pill ${r.status==='completed'?'status-completed':r.status==='skipped'?'status-skipped':'status-not-started'}">${r.status==='completed'?'Done':r.status==='skipped'?'Skipped':'Pending'}</span></div><div class="search-result-date">${dd}${r.notes?' · '+r.notes:''}</div></div>`; }).join(''); });

            document.getElementById('btnSaveSettings').onclick = async () => { const name = document.getElementById(
                    'settingTeacherName').value.trim() || 'Mr. Mithilesh'; const dm = document.getElementById(
                    'toggleDarkMode').checked; await dbPut('settings', { key: 'app', teacherName: name, darkMode: dm });
                document.getElementById('topbarTeacherName').textContent = name;
                document.getElementById('printOnlyTeacher').textContent = name;
                applyDarkMode(dm);
                toast('Settings saved! 💾'); };
            document.getElementById('toggleDarkMode').onchange = function() { applyDarkMode(this.checked); };
            document.getElementById('darkModeToggleRow').addEventListener('click', function(e) { if (e.target.tagName ===
                    'INPUT') return; const cb = document.getElementById('toggleDarkMode');
                cb.checked = !cb.checked;
                applyDarkMode(cb.checked); });
            document.getElementById('btnExportData').onclick = async () => { const data = { progress: await dbGetAll(
                        'progress'), schedule: await dbGetAll('schedule'), settings: await dbGetAll('settings') }; const
                    blob = new Blob([JSON.stringify(data, null, 2)], { type: 'application/json' }); const a = document
                    .createElement('a');
                a.href = URL.createObjectURL(blob);
                a.download = `RPS_Backup_${dateStr(TODAY)}.json`;
                a.click();
                toast('Data exported! 📤'); };
            document.getElementById('btnImportData').onclick = () => document.getElementById('importFileInput').click();
            document.getElementById('importFileInput').onchange = async function() { const file = this.files[0]; if (!file)
                    return; const text = await file.text(); try { const data = JSON.parse(text); if (!data.progress || !data
                        .schedule) throw new Error('Invalid format'); if (!confirm(
                            'Import will replace all data. Continue?')) return; await dbClear('progress'); await dbClear(
                        'schedule'); await dbClear('settings'); for (const p of data.progress) await dbPut('progress',
                    p); for (const s of data.schedule) await dbPut('schedule', s); for (const st of (data.settings || [
                    ])) await dbPut('settings', st);
                    toast('Imported! Reloading... 📥');
                    setTimeout(() => location.reload(), 800); } catch (e) { toast('Import failed: ' + e.message); } this
                    .value = ''; };
            document.getElementById('btnResetData').onclick = async () => { if (!confirm(
                        'Delete ALL data? Cannot undo.')) return; if (prompt('Type "DELETE" to confirm:') !== 'DELETE')
                    return; await dbClear('progress'); await dbClear('schedule'); await dbClear('settings');
                toast('Reset! Reloading... 🗑️');
                setTimeout(() => location.reload(), 800); };

            function switchView(view) { currentView = view;
                document.querySelectorAll('.view').forEach(v => v.classList.remove('active')); const t = document
                    .getElementById('view-' + view); if (t) t.classList.add('active');
                document.querySelectorAll('.sidebar-nav button').forEach(b => b.classList.remove('active')); const nb = document
                    .querySelector(`[data-view="${view}"]`); if (nb) nb.classList.add('active'); if (view === 'analytics')
                    renderAnalytics(); if (view === 'calendar') renderCalendar(); if (view === 'today') renderToday(
                    currentDate);
                closeSidebar(); }
            document.querySelectorAll('.sidebar-nav button').forEach(b => b.addEventListener('click', () => switchView(b
                .dataset.view)));
            const sidebar = document.getElementById('sidebar'),
                overlay = document.getElementById('sidebarOverlay');
            document.getElementById('hamburgerBtn').onclick = () => { sidebar.classList.toggle('open');
                overlay.classList.toggle('active'); };
            overlay.onclick = () => { sidebar.classList.remove('open');
                overlay.classList.remove('active'); };
            window.addEventListener('resize', () => { if (window.innerWidth > 900) { sidebar.classList.remove('open');
                    overlay.classList.remove('active'); } });

            function closeSidebar() { sidebar.classList.remove('open');
                overlay.classList.remove('active'); }

            function formatDateNice(d) { const dt = parseDateSafe(d); if (!dt) return d || 'Unknown'; const days = ['Sunday',
                    'Monday', 'Tuesday', 'Wednesday', 'Thursday', 'Friday', 'Saturday'
                ]; const months = ['January', 'February', 'March', 'April', 'May', 'June', 'July', 'August', 'September',
                    'October', 'November', 'December'
                ]; return `${days[dt.getDay()]}, ${dt.getDate()} ${months[dt.getMonth()]} ${dt.getFullYear()}`; }

            function formatDateShort(d) { const dt = parseDateSafe(d); if (!dt) return d || 'Unknown'; const months = ['Jan',
                    'Feb', 'Mar', 'Apr', 'May', 'Jun', 'Jul', 'Aug', 'Sep', 'Oct', 'Nov', 'Dec'
                ]; return `${months[dt.getMonth()]} ${dt.getDate()}, ${dt.getFullYear()}`; }

            function toast(msg) { const el = document.getElementById('toast');
                el.textContent = msg;
                el.classList.add('show');
                clearTimeout(el._timeout);
                el._timeout = setTimeout(() => el.classList.remove('show'), 2500); }
            document.addEventListener('keydown', e => { if (e.key === 'Escape') { document.getElementById('editModalOverlay')
                        .classList.add('hidden');
                    curEditId = null;
                    curEditPrevStatus = 'not-started';
                    closeSidebar(); } });

            (async () => { await openDB(); await initProgress(); await initSettings();
                document.getElementById('topbarDate').textContent = formatDateShort(TODAY);
                document.getElementById('printOnlyDate').textContent = formatDateNice(TODAY); await renderToday(currentDate); })();
        })();
    </script>
</body>
</html>
